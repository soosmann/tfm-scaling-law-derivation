# Scaling Law Derivation

This repo contains scripts for deriving scaling laws from json files that contain curve data. 
Is thought to interact with json data generated in the repos `tfm-lcbench-preprocessing` and `tfm-natsbench-preprocessing`.
I use Python 3.12 for this project.